#include<stdio.h>
int main()
{
	int a[]={2,2,8,6,4};
	int len=sizeof(a)/sizeof(int);
	int i,count=0,t,n=0;
	t=a[0];
	for(i=n;i<len;i++)
	{
		if(t==a[i])
		{
			count++;
			break;
		}
	}
	if(count>0)
		printf("DUPLICATE");
	else
		printf("UNIQUE");
}
